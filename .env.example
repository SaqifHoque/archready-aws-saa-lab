# The web UI remains bound to localhost. Change this only if 42873 is occupied.
ARCHREADY_PORT=42873

# Used only inside the private Docker network; PostgreSQL has no host port.
ARCHREADY_DB_NAME=archready
ARCHREADY_DB_USER=archready
ARCHREADY_DB_PASSWORD=replace-with-a-long-local-password
