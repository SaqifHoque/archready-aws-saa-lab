// Replace these values with the outputs from `sam deploy` to enable account sync.
// Until then, ArchReady continues to save progress locally in this browser.
window.ARCHREADY_CLOUD = {
  enabled: false,
  apiUrl: '',
  cognitoDomain: '',
  clientId: '',
  redirectUri: '',
  logoutUri: '',
};
